import React from 'react'

export default function AppFooter() {
  return (
    <footer className="mt-8 text-xs text-gray-500">
      Built with React + Tailwind + D3 + ELK.js. Diagrams are static and deterministic for easy embedding.
    </footer>
  )
}
