import React from 'react'

export default function AppFooter() {
  return (
    <footer className="mt-10 text-xs text-slate-300 text-center">
      Built with React + Tailwind + D3 + ELK.js — static, deterministic diagrams for easy embedding.
    </footer>
  )
}
